import hero from '../assets/hero-img.jpg';

const Hero = () => {
	return (
		<div className=''>
			<div>
				<img src={hero} alt='' className='h-[100vh] w-full' />
			</div>
		</div>
	);
};

export default Hero;
